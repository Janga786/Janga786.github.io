# Jangara Bliss — Robotics Portfolio

An evidence-first research portfolio for Jangara Bliss, a computer engineering student focused on robot learning and autonomous systems. The site is intentionally explicit about what was completed in simulation, what ran on hardware, what remains active research, what came from a team or course, and which upstream systems made the work possible.

The public research narrative foregrounds three studies:

- Booster K1 / NaVILA navigation research — complete 1,077-episode simulation benchmark and active viewpoint study
- xembench — executed cross-embodiment manipulation benchmark and intervention campaign
- CLiDA / LiDAR motion analysis — attributed five-person sensing project with controlled final trials

Selected Systems & Engineering separately covers the NASA hexapod, KUKA kinematics, legacy robot restoration, and embedded/FPGA/PCB work. The complete archive also preserves the KDUR data platform.

## Tech stack

- Next.js 16 App Router with static export (`output: "export"`)
- TypeScript and Tailwind CSS v4
- Framer Motion for restrained reveals
- GitHub Pages deployment from `out/`

## Local development

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Validation

```bash
npm run lint
npm run build
```

The production site is emitted to `out/`. There is no server runtime.

The downloadable résumé is generated from evidence-aligned source copy:

```bash
/Users/janga/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 \
  scripts/generate_resume.py
```

## Content architecture

All public copy lives under `content/`:

- `profile.ts` — bio, three research questions, education, experience, capabilities, and leadership
- `projects.ts` — case studies, methods, experimental design, metrics, attribution, failure analysis, limitations, and next questions
- `writing.ts` — real research outputs with authorship and status labels; no planned-content cards
- `site-config.ts` — canonical URL and visibility toggles

Project pages use distinct status labels including active research, completed study, fielded system, and validated library. Visible attribution fields identify the applicant's role, advisor when applicable, collaborators, and upstream systems/models. Separate provenance notes preserve team ownership, course context, inherited code, AI-assisted implementation, and simulation/hardware boundaries.

## Media and evidence

- `public/diagrams/` contains deterministic SVG system diagrams.
- `public/media/` contains project photographs, videos, and report-derived figures.
- `public/og/k1-navigation-sim.png` is a real project frame used by the homepage social card; each project route also generates a project-specific social card.
- `public/resume.pdf` is generated by `scripts/generate_resume.py`.

External artifacts link to the canonical GitHub repository, result report, evidence ledger, or methods documentation. Files without a defensible public source are not presented as evidence.

## Deployment

The repository targets GitHub Pages. Pushing to `main` triggers `.github/workflows/deploy.yml`, which runs `npm ci && npm run build` and publishes `out/`.

If the production domain changes, update `siteUrl` in `content/site-config.ts`.

## Evidence policy

- Do not convert a hardware clip into a model-control claim without synchronized command or model traces.
- Do not publish a detector metric without a preserved held-out split and result artifact.
- Distinguish designed, fabricated, assembled, and validated hardware milestones.
- Name team roles, course origins, upstream frameworks, inherited code, and AI-assisted implementation.
- Preserve denominators, per-episode artifacts, and negative results.
- Keep limitations visible; an unsupported number is removed, not softened.
